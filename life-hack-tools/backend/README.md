# Backend — Brahma Muhurta API

This is the Node.js backend for the MomThings project.  
It scrapes Brahma Muhurta timings for the next 30 days and exposes them via an API.

## Endpoint

### GET /api/brahma
Returns an HTML table containing Brahma Muhurta timings for the next 30 days.

## Local Development

 


## Deployment (Render)

- Create a new Web Service
- Connect this repo
- Set root directory to `/backend`
- Build command: `npm install`
- Start command: `npm start`
